import { SpecialAdminValidatorDirective } from './admin-email-validator.directive';

describe('SpecialAdminValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialAdminValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
